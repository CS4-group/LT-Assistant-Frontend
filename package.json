{
  "name": "lt-assistant-frontend-vanilla",
  "version": "0.0.0",
  "description": "LT Assistant - Pure HTML/CSS/JavaScript version (NO dependencies)",
  "scripts": {
    "start": "python -m http.server 8000",
    "serve": "python -m http.server 8000"
  },
  "dependencies": {},
  "devDependencies": {},
  "keywords": ["vanilla", "html", "css", "javascript", "no-build"],
  "engines": {
    "note": "No Node.js required - runs in any browser with a simple HTTP server"
  }
}